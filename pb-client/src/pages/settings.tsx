import { PagePlaceholder } from "../components/PagePlaceholder";

export default function Settings() {
	return (
		<PagePlaceholder
			description="Tune workspace preferences, account behavior, and defaults for new polls."
			icon="settings"
			label="Settings"
			title="Workspace settings"
		/>
	);
}
